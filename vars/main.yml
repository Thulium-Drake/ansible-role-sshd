---
admin_ssh_keys: "{{ lookup('community.general.merge_variables', '_admin_ssh_keys__to_merge', pattern_type='suffix', initial_value=[]) }}"
sshd_configs: "{{ lookup('community.general.merge_variables', '_sshd_configs__to_merge', pattern_type='suffix', initial_value=[]) }}"
